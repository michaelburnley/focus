public enum Action {
    RIGHT,
    LEFT,
    UP,
    DOWN,
    FOCUS,
    ATTACK1,
    ATTACK2,
    ATTACK3,
}

public enum Directional {
    NONE,
    LEFT = Action.LEFT,
    RIGHT = Action.RIGHT,
}